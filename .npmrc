# Expo + Cloudflare Workers モノレポでの依存解決トラブルを避けるため
# pnpmデフォルトの isolated ではなく hoisted を採用。
# 参照: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
